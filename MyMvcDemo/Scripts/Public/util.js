 define(function (require, exports, module) {     var o = {};      o.optsToStr = function (opts) {         var str = "";         if (opts) {             var index = 0;             for (var name in opts) {                 str += (index == 0 ? "?" : "&" ) + name + "=" + opts[name] ;                 index++;             }         }         return str;     };      module.exports = o; });  