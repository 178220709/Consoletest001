@using Microsoft.Ajax.Utilities
@using JsonSong.ManagerUI.Extend

@{
    var heads = new List<IndexColumnModel>()
    {
        new IndexColumnModel() {Name = "avalon Hello", Url = Url.Action("Index", new {pageName = "Hello"}), Description = "简单介绍"},
        new IndexColumnModel() {Name = "avalon Bind", Url = Url.Action("Index", new {pageName = "Bind"}), Description = "双向绑定"},
        new IndexColumnModel() {Name = "avalon Base", Url = Url.Action("Index", new {pageName = "Base"}), Description = "面向数据编程"},
        new IndexColumnModel() {Name = "avalon Demo", Url = Url.Action("Index", new {pageName = "Demo"}), Description = " 复杂页面设计"}
    };
    var module = ControllerHelper.GetControllerActions(this);
}

@Html.Partial("_IndexHead", heads)

